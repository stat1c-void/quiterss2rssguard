# QuiteRSS database version

```sqlite
CREATE TABLE info
(
    id    integer primary key,
    name  varchar,
    value varchar
);
```

Target version data:

```text
   id = 1
 name = version
value = 17
```
